env:
  - GO111MODULE=on
before:
  hooks:
    - go mod tidy
project_name: masscan-go
builds:
  - id: masscan-go-linux-arm64
    ldflags:
      - -s -w
    binary: masscan-go
    env:
      - CGO_ENABLED=1
    main: ./cmd/masscan-go/masscan-go.go
    goos:
      - linux
    goarch:
      - arm64

archives:
  - formats:
      - zip
    name_template: '{{ .ProjectName }}_{{ .Version }}_{{ if eq .Os "darwin" }}macOS{{ else }}{{ .Os }}{{ end }}_{{ .Arch }}'

checksum:
  name_template: "{{ .ProjectName }}-linux-arm64-checksums.txt"

announce:
  slack:
    enabled: false
    channel: '#release'
    username: GoReleaser
    message_template: 'New Release: {{ .ProjectName }} {{.Tag}} is published! Check it out at {{ .ReleaseURL }}'

  discord:
    enabled: false
    message_template: '**New Release: {{ .ProjectName }} {{.Tag}}** is published! Check it out at {{ .ReleaseURL }}' 